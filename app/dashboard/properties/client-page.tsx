// Make sure the file properly imports and uses PropertiesClient

"use client"

import PropertiesClient from "./properties-client"

export default function PropertiesClientPage() {
  return <PropertiesClient />
}
